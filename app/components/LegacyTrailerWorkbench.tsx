"use client";

// Kept as a stable import target for older integrations. The public app uses
// the current workbench and does not expose a second legacy interface.
export { default } from "./TrailerWorkbench";
